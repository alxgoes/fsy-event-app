import { MediaManager } from "@/components/admin/MediaManager";

export const metadata = {
  title: "Gestão de Fotos & Mídia — FSY Admin",
};

export default function AdminMediaPage() {
  return <MediaManager />;
}
